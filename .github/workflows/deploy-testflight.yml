name: Deploy TestFlight

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: production-ios
  queue: max

permissions:
  contents: read

jobs:
  ios:
    name: iOS TestFlight
    if: github.ref == 'refs/heads/main' || github.event_name == 'workflow_dispatch'
    runs-on: macos-26
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4

      - name: Check release credentials
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          APP_STORE_CONNECT_API_KEY: ${{ secrets.APP_STORE_CONNECT_API_KEY }}
        run: |
          : "${EXPO_TOKEN:?Set the EXPO_TOKEN repository secret}"
          : "${APP_STORE_CONNECT_API_KEY:?Set the APP_STORE_CONNECT_API_KEY repository secret}"

      - uses: actions/setup-node@v4
        with:
          node-version: "24"

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.2"

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"

      - name: Install dependencies
        run: |
          bun install --frozen-lockfile
          bun install --global eas-cli@18.6.0
          gem install fastlane cocoapods --no-document

      - name: Check app
        run: |
          bun run typecheck
          bun run test
          python3 scripts/check-ios-branding.py

      - name: Build iOS on this runner
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          NODE_ENV: production
        run: |
          eas build \
            --platform ios \
            --profile production \
            --local \
            --output "$RUNNER_TEMP/flashfront.ipa" \
            --non-interactive

      - name: Verify the signed app name and icon
        run: python3 scripts/check-ios-branding.py "$RUNNER_TEMP/flashfront.ipa"

      - name: Save the verified iOS build
        uses: actions/upload-artifact@v4
        with:
          name: shuffle-ios
          path: ${{ runner.temp }}/flashfront.ipa
          retention-days: 7

      - name: Upload to TestFlight
        env:
          APP_STORE_CONNECT_API_KEY: ${{ secrets.APP_STORE_CONNECT_API_KEY }}
        run: |
          umask 077
          trap 'rm -f "$RUNNER_TEMP/app-store-connect.json"' EXIT
          printf '%s' "$APP_STORE_CONNECT_API_KEY" > "$RUNNER_TEMP/app-store-connect.json"
          fastlane pilot upload \
            --ipa "$RUNNER_TEMP/flashfront.ipa" \
            --apple_id "$(jq -er '.submit.production.ios.ascAppId' eas.json)" \
            --api_key_path "$RUNNER_TEMP/app-store-connect.json" \
            --skip_waiting_for_build_processing true
